# 🧠 Neural Sage Bot (RAG + Web Search)

**Neural Sage** — это продвинутый Telegram-бот, который использует локальные нейросети (LLM) для ответов на вопросы. Бот умеет искать актуальную информацию в интернете и работать с вашей локальной базой знаний (документами), используя технологию RAG (Retrieval Augmented Generation).

## ✨ Особенности

- 🔒 **Полная приватность:** Все нейросети работают локально на вашем ПК.
- 🔎 **Web Search:** Умеет гуглить и отвечать на основе свежих данных.
- 📚 **RAG:** Читает ваши текстовые файлы и отвечает по ним.
- 🚀 **Асинхронность:** Быстрая работа благодаря `aiogram` и асинхронной архитектуре.

---

## 🛠 Требования к системе

Для работы бота вам понадобится:
- **Python 3.10** или выше.
- **Ollama** (для запуска локальных нейросетей).
- Примерно **8-16 ГБ ОЗУ** (зависит от выбранной модели).
- **Telegram Bot Token** (от BotFather).

---

## 🚀 Установка и запуск

Следуйте этим шагам, чтобы запустить бота на своем компьютере (Windows/Linux/macOS).

### Шаг 1. Установка и настройка Ollama

Бот использует **Ollama** как движок для нейросетей. Это приложение запускает локальные языковые модели.

1. Скачайте Ollama с официального сайта: [ollama.com/download](https://ollama.com/download)
2. Установите приложение и запустите его.
3. Ollama создаст фоновый сервис на порту 11434 (не закрывайте его).

**Загрузите основную модель для общения (Neural Chat):**
Откройте терминал/cmd и выполните:
```bash
ollama pull neural-chat
```

**Загрузите модель для работы с базой знаний (Embeddings):**
```bash
ollama pull nomic-embed-text
```

Обе команды начнут скачивание моделей. Первое скачивание может занять несколько минут (зависит от интернета).

> ⚠️ **Важно:** Убедитесь, что приложение Ollama запущено в фоновом режиме перед стартом бота. Если процесс Ollama остановится, бот не сможет работать.

---

### Шаг 2. Скачивание проекта

Скопируйте все файлы проекта в одну папку или клонируйте репозиторий:

```bash
git clone https://github.com/YOUR_USERNAME/Neural_Sage_Bot.git
cd Neural_Sage_Bot
```

---

### Шаг 3. Настройка окружения Python

Рекомендуется использовать виртуальное окружение, чтобы не конфликтовать с глобальными пакетами.

**Для Windows (PowerShell):**
```bash
python -m venv venv
venv\Scripts\activate
```

**Для Windows (CMD):**
```bash
python -m venv venv
venv\Scripts\activate.bat
```

**Для Linux/macOS:**
```bash
python3 -m venv venv
source venv/bin/activate
```

После активации виртуального окружения установите все зависимости:

```bash
pip install -r requirements.txt
```

Команда установит все необходимые библиотеки:
python-telegram-bot==20.7 — официальная библиотека для работы с Telegram API
ollama>=0.1.0 — Python клиент для интеграции с локальными нейросетями через Ollama
chromadb==0.5.3 — векторная база данных для хранения эмбеддингов документов
requests==2.31.0 — HTTP библиотека для веб-поиска и запросов
beautifulsoup4==4.12.3 — парсинг HTML при поиске информации в интернете
python-dotenv==1.0.0 — загрузка переменных окружения из файла .env
psutil>=5.9.0 — мониторинг использования CPU и памяти системы
httpx>=0.27.0 — асинхронные HTTP запросы для быстрой работы
aiohttp>=3.9.0 — асинхронные операции для async/await функций бота

---

### Шаг 4. Настройка конфига (.env)

1. В папке проекта найдите файл `file.env`.
2. **Переименуйте его в `.env`** (удалите приставку "file").

```bash
ren file.env .env
```

3. Откройте файл `.env` текстовым редактором (Блокнот, VS Code, Sublime).
4. Найдите строку `BOT_TOKEN` и замените `YOUR_TOKEN` на **ваш настоящий Telegram Bot Token**.

**Где взять BOT_TOKEN:**
- Откройте Telegram и найдите бота **@BotFather**.
- Нажмите `/start` и следуйте инструкциям для создания нового бота.
- BotFather вам выдаст токен вида `123456789:AAHk...`.
- Скопируйте этот токен в `.env` файл.

> **Остальные параметры** можно оставить по умолчанию, если вы только начинаете. Они настроены для стандартной работы.

---

### Шаг 5. Запуск бота

Теперь всё готово! Убедитесь, что:
1. ✅ Ollama запущена и слушает на `localhost:11434`.
2. ✅ Виртуальное окружение активировано.
3. ✅ `.env` файл с `BOT_TOKEN` на месте.

В терминале выполните:
```bash
python main.py
```

Если вы увидите сообщение вроде:
```
✅ Bot started successfully!
🤖 Listening for messages...
```

Значит, бот запущен и готов работать! 🎉

Откройте Telegram, найдите вашего бота и отправьте ему сообщение `/start`.

---

## 📁 Структура проекта

```
neural-sage-bot/
├── main.py                          # Точка входа, запуск бота
├── config.py                        # Конфигурация приложения
├── file.env                         # Шаблон конфига (переименуй в .env)
├── requirements.txt                 # Зависимости Python
├── core_llm_service.py              # Интеграция с Ollama (LLM)
├── core_embeddings_service.py       # Embeddings для поиска по документам
├── core_web_search_service.py       # Поиск информации в интернете
├── core_database_manager.py         # Работа с векторной БД (ChromaDB)
├── core_init.py                     # Инициализация модуля core
├── processor_rag_pipeline.py        # RAG пайплайн (поиск + генерация)
├── telegram_bot_handlers.py         # Обработчики сообщений
├── telegram_bot_keyboards.py        # Кнопки и клавиатуры
├── telegram_bot_message_formatter.py# Форматирование ответов
└── logger.py                        # Логирование событий
```

---

## 📚 Как использовать базу знаний (RAG)

Бот использует локальную векторную базу данных ChromaDB для хранения информации из ваших документов. Эта база создается автоматически при первом запуске.

Как добавить свои документы:
1. Бот автоматически ищет текстовые файлы в папке проекта (или в папке data, если она настроена в коде).
2. При запуске бот проиндексирует все найденные .txt файлы и сохранит их в chroma_db (папка создается автоматически).
3. Теперь когда вы зададите вопрос, бот будет ссылаться на вашу базу знаний и отвечать на основе ваших документов.
4. База данных — это просто папка с файлами, вам не нужно ничего устанавливать дополнительно.

Примечание: Без документов бот все равно будет работать — он будет искать информацию в интернете и использовать знания нейросети.

---

## 🔧 Возможные проблемы и решения

### ❌ Ошибка: "ConnectionError: Cannot connect to Ollama"
**Решение:** Убедитесь, что Ollama запущена. Откройте приложение Ollama или выполните в терминале:
```bash
ollama serve
```

### ❌ Ошибка: "Model not found: neural-chat"
**Решение:** Загрузите модель:
```bash
ollama pull neural-chat
```

### ❌ Бот не отвечает, ошибка в логах
**Решение:** Проверьте `BOT_TOKEN` в `.env` файле. Убедитесь, что это правильный токен от BotFather.

### ❌ Низкая скорость ответов
**Решение:** Уменьшите модель или увеличьте ОЗУ. Если есть GPU, рассмотрите использование его через Ollama.

---

## 🤝 Вклад в проект

Если у вас есть идеи по улучшению или вы нашли баг:
1. Создайте **Issue** с описанием проблемы.
2. Сделайте **Fork** репозитория.
3. Создайте ветку: `git checkout -b feature/your-feature`.
4. Сделайте коммит: `git commit -m "Добавил новую фичу"`.
5. Отправьте **Pull Request**.

---

## 📄 Лицензия

Этот проект распространяется под лицензией **MIT**. Используйте свободно!

---

## ❓ Вопросы и поддержка

Если что-то не работает или у вас есть вопросы:
- Откройте **Issue** на GitHub.
- Проверьте логи бота (они выводятся в консоль).
- Убедитесь, что все зависимости установлены: `pip list`.

---

## 📞 Контакты

- **GitHub:** [ZiFFiR4iK](https://github.com/ZiFFiR4iK)
- **Telegram:** [@Jacobs_Monarh](https://t.me/Jacobs_Monarh)

---

**Спасибо за использование Neural Sage Bot! 🎉**
Happy coding! 🚀
